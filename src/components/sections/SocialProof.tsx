import React from 'react'

const SocialProof: React.FC = () => {
  return (
    <section id="social-proof" className="section-padding bg-black">
      <div className="container-max">
        <h2 className="text-4xl font-bold mb-12 text-center">Lo que dicen de nosotros</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {/* Testimonios irán aquí */}
        </div>
      </div>
    </section>
  )
}

export default SocialProof
